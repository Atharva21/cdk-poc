def test_sample():
    result = {
        'statusCode': 200,
        'body': 'published message to SQS'
    }
    assert result == {
        'statusCode': 200,
        'body': 'published message to SQS'
    }
